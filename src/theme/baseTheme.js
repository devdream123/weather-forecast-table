const fontSize = {
  sm: '14px',
  md: '16px',
  lg: '18px',
  xl: '20px',
};

const color = {
  grey: '#303030',
  lightGrey: '#BDBDBD',
  lighterGrey: '#D3D3D3',
};

const spacing = {
  sm: '10px',
  md: '15px',
  lg: '30px',
};

const baseTheme = {
  color,
  spacing,
  fontSize,
};

export default baseTheme;
